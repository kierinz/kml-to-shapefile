name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 */10 * * *' # Berjalan setiap 10 jam
  workflow_dispatch: # Membolehkan anda jalan secara manual

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5 # Ditukar ke v5 untuk menghilangkan amaran pepijat Node 20/24
        with:
          token: ${{ secrets.STREAMLIT_TOKEN }}

      - name: Make empty commit
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git commit --allow-empty -m "Keep-alive: Sentiasa Aktif"
          git push
